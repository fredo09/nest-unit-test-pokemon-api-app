/**
 * This is the Pokemon entity class that represents a Pokemon in the application.
 * It contains the properties of a Pokemon, including its id, name, type, hp, and sprites.
 */
export class Pokemon {
  id!: number;
  name!: string;
  type!: string;
  hp!: number;
  sprites!: string[];
}
